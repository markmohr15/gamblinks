<% if @link.votes_count != 0 %>
  var html = "<%= @link.votes_count + 1 %> Likes";
 <% else %>
  var html = "1 Like";
<% end %>

var container = $("#link-<%= @vote.link.id %>");

container.find(".count").text(html)
container.find('input[type=submit]').attr('disabled', true)
